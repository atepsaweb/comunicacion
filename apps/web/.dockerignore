# Excluir directorios que no deben entrar al build context de Docker.
# Sin este archivo el contexto incluye node_modules (600MB+) y ralentiza el build.
node_modules
.next
.git
*.md
.env*
!.env.example
coverage
.turbo
